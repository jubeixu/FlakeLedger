## Motivation

<!-- Why is this change needed? Link the issue if there is one. -->

## What changed

<!-- One behaviour per PR. List the concrete changes. -->

## Checklist

- [ ] `pytest -q` passes
- [ ] Rendered ledger is byte-identical for the same inputs (golden tests)
- [ ] No new dependencies, no network access
- [ ] New JUnit dialect has a fixture in `samples/` and tests
- [ ] Cost rates are declared inputs, not hard-coded
